default idnfile = "src/test/resources/ingest-idns.txt";
default alg = "sum";
default table = "test";

idnfile|
open("file")|
as-lines|
template("https://portal.dnb.de/opac.htm?method=requestMarcXml&idn=${obj}")|
open("http")|
read("marcxml")|
morph("mapping/ingest.marc21.xml")|
to-hbase(table + "@" + host);